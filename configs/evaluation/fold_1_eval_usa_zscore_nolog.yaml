# Evaluation configuration for fold 1 - All Features Experiment
# Evaluates models trained on train_fold_1.txt against test_fold_1.txt
# Dataset: CARAVAN_TRANSFORMED_WITH_ALL_FEATURES_eval

data:
  # Path to evaluation dataset
  base_path: ../camels-usa-transformed-zscore-nolog_eval

  # Test basins for fold 1
  gauge_ids_file: configs/basin_ids_files/test_fold_1.txt

  # Pipeline path (uses same pipeline as training)
  pipeline_path: ../camels-usa-transformed-zscore-nolog_eval/ts_pipeline.joblib
